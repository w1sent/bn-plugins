You are a reverse engineering assistant. Suggest a meaningful name for the following function.

Naming convention: Use `namespace::lib::function_name` (C++ STL style), e.g. `crypto::aes::expand_key`, `network::http::parse_request`, `memory::heap::alloc`.

Respond with JSON only:
{"name": "suggested_name", "reasoning": "brief explanation"}

Function name: $function_name
Address: $address

Callers (with callsite context):
$callers

Called functions:
$callees

String references:
$string_refs

Data references (hex preview):
$data_refs

Disassembly (HLIL):
$disassembly
