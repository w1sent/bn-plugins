# Plugin settings: BN Settings UI + per-plugin README

Settings are registered via BN's native `Settings` API and appear in BN's
Settings dialog for discovery and editing. Each plugin also has a `README.md`
that documents the meaning, valid values, and examples for each setting —
BN's Settings UI shows only the key and value, not a description.